@model HotelGolfBooking.Models.golf

@{
    ViewBag.Title = "Delete";
}
<h3>Bạn có chắc chắn xóa sân golf này?</h3>
<fieldset>
    <label for="room" style="width:auto;display:block;">sân golf @Model.name</label>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" class="btn btn-info" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
