@model HotelGolfBooking.Models.golf_rule

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>golf_rule</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.rulegolf)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.rulegolf)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
